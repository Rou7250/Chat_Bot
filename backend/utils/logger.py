# logger.py

def log_query(query, chunks, scores, answer, confidence, mode):
    # Dummy implementation for missing utils.logger module
    # Replace with actual logic as needed
    print(f"LOG: {query}, {chunks}, {scores}, {answer}, {confidence}, {mode}")
